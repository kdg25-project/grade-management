# Local Worker secrets are read from .dev.vars, not from this file.
# Copy .dev.vars.example to .dev.vars and replace the secret before `bun run dev`.
BETTER_AUTH_SECRET=replace-with-a-random-secret-at-least-32-characters-long
