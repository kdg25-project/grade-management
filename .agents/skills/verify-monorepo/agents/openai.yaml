interface:
  display_name: "Monorepo検証"
  short_description: "Bun・Vite・Hono Workers・D1・Email Sending構成を検証する"
  default_prompt: "Use $verify-monorepo to verify the Vite React SPA, Hono Worker, D1 migrations, Email Sending binding, and Workers Static Assets routing; report actionable failures."
