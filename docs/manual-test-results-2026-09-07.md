# 手動テスト結果（2026-09-07）

| 項目 | 内容 |
|---|---|
| 実施日 | 2026-09-07 |
| 環境 | 本番URL（`https://grade-management.tah5882.workers.dev`） |
| 初回ログイン・CSV・境界入力 | commit `7a2632d` のデプロイ前に実施 |
| 評価表示・再試験・出力等 | commit `7a2632d` のデプロイ後に実施 |
| 6410611時点の最新UI | commit `6410611` のデプロイ成功。version `904fb16f-f2ba-4db7-a5a6-1d6e1b4c1122`。追加CSV toast実装はこの時点に含まれず、本番未反映。 |
| 現行修正版 | commit `80b14d2` のpush/deploy成功。version `88ce7fcf-c0e9-47c1-b412-bf04d9ecb7b1` |
| 個人情報・秘密情報 | 実学生の氏名・学籍番号、認証情報、秘密値は記載していない |
| 注記 | 手動確認の時点を分けて記載する。コードテスト・自動テストの結果は別証拠欄に分ける |

## 確認済み

| 分類 | 実施内容・結果 |
|---|---|
| 認証・権限 | admin と QA teacher でログインできた。teacher で管理画面へ直接アクセスすると拒否された。 |
| マスタCSV | 講師10件・学生30件・科目15件を登録できた。スタッフCSVは既存データを含む上限により拒否された。 |
| 通常CSV再取込 | 最新 `6410611` で指定講師名簿10件をプレビューし、問題がないことを確認した。ユーザー確認後に反映が成功し、「反映しました。再送は行いません。」と表示され、「反映済み」ボタンがdisabledになった。以前のJS exceptionも再現しなかった。登録済みCSVの再取込とUI二重送信防止の確認であり、同一API request keyの強制replayではない。 |
| 現行CSV apply | 本番IAB DOMで講師CSV10件の反映完了、「反映済み」ボタンdisabled、kind/file/previewがdisabledであることを確認した。成功インライン帯と`role=status`は現在表示されていない。toast表示の瞬間はCDP deadlineで取得できず、本番での表示瞬間の証拠は未取得。toast表示そのものはE2E 7件通過で確認した。 |
| 評価比重 | 各値0〜100の整数、合計100を確認。合計99は拒否され、60/20/20は保存できた。 |
| 成績入力 | 出席率101、授業態度0は拒否された。保存後の再読込で値が保持された。 |
| 評価境界 | 90=秀、80=優、70=良、60=可、59.4=不可、89.4=優、79.4=良、69.4=可を確認した。 |
| 確定・遷移 | 確定後、teacher の前期URLを開くと後期へリダイレクトされた。 |
| 管理者修正 | テスト学生の80/8/7（78・良）を90/8/7（84・優）へ修正し、保存と監査記録を確認した。 |
| 再開・再確定 | CUAによるAsideの直接操作で、理由空欄の再開は拒否された。理由入力後は再開でき、再確定も成功した。 |
| 再試験 | テスト科目の不可59.4から、80/8/8（80・優）を再試験登録できた。2回目の優、1回目の不可、▲表示を維持した。最新評価が不可以外の場合は再試験ボタンが無効になることを確認した。 |
| 入力人数 | 科目未入力の入力人数が0/15で表示された（旧15/15表示の修正を本番で確認）。 |
| 入学年度validation | 架空の検証入力（実個人情報なし）で入学年度2023を指定したところ、inline alert「入学年度が登録されていません。年度管理で追加してください。」が表示された。generic 500は発生せず、学生も登録されなかった。検証入力は空欄に戻した。 |
| CSV/PDF出力 | 実CSV・PDFを両方15件で出力した。Downloadsで `成績一覧_2024年度.csv` と `成績一覧_2024年度.pdf` を確認した。両方の評価件数は秀1・優4・良8・可2で一致した。PDFは1ページで、全項目を目視して文字切れがないことを確認した。 |
| 年度更新プレビュー（7a2632d/6410611時点） | 2025年度の講師名簿・科目一覧・学生名簿を確認した。2年・3年の科目をスキップでき、旧 `undefined` JS exception は再現しなかった。当時はヘッダー不一致が1行エラーとして表示され、一括反映が無効になった。この結果は別作業 `2aa4371` のCSVヘッダー変更前の履歴であり、現行の問題とは断定しない。 |
| テスト環境 | Chrome browser missing と AsideMCP transport の問題はテスト環境に限定され、CUAによるAsideの直接操作で主要な確認を継続できた。 |
| 6410611時点のtoast/dropdown追加UI | commit `6410611` のデプロイ成功後、本番 `/admin/subjects` でlongQAteacherを選択した。メールアドレスは記載しない。select幅216.33px、右端725.99px、カード右端751pxでfits=trueだった。「通知確認科目0907」を登録し、`role=status` の科目登録成功文言が `parenttoastViewport` に表示された。自動消去は `waitFor detached` 成功、通知文字が画面から消えた後も登録行が残ることを確認した。 |

## 別証拠（コード・自動テスト）

| 区分 | 結果 |
|---|---|
| 6410611時点の全テストコード | 286件通過（31 scripts、80 web、13 db、162 api）。手動本番結果とは別の証拠として扱う。 |
| 6410611時点のE2E | 7件通過。手動本番結果とは別の証拠として扱う。現行の追加CSV toast実装のE2E結果とは別扱い。 |
| 現行の追加CSV toast実装 | API 163件となり、fulltest 287 pass、typecheck、build、localmigrate、dryrunは通過。commit `80b14d2` でE2E 7件が15.4秒で通過し、独立reviewerも再レビューでAPPROVEした。前回の失敗は、比重保存後GETを成績保存後GETと取り違えた待受raceで、specのみ修正して解消した。6410611時点のE2E 7件通過とは時点を分けて記載する。 |

## 未実施・追加確認が必要

| 項目 | 状態 |
|---|---|
| 年度更新反映 | 2025年度のプレビューまで確認。一括反映（apply）は未実行。 |
| オフライン・複数セッション | 10分オフラインを含む正式な dual-session テストは未実施。 |

コードのテスト結果と本番画面での実測結果は別の証跡として扱う。本記録の確認済み欄は上記の本番操作で確認した範囲に限る。
